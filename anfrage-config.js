// Beim Hosting einmalig setzen, z. B. auf eine Supabase Edge Function oder eigene API.
// Der Endpunkt erhält multipart/form-data mit "payload" (JSON) und optional "logo" (Datei).
window.NOBANO_FORM_CONFIG = {
  submitUrl: ""
};
